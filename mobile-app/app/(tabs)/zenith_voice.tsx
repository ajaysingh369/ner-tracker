import { Redirect } from 'expo-router';
// This file exists only to satisfy the Expo Router requirement for the custom tab button
export default function ZenithVoicePlaceholder() {
    return <Redirect href="/" />;
}
